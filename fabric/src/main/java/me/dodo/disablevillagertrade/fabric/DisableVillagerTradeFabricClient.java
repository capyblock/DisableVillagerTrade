package me.dodo.disablevillagertrade.fabric;

import net.fabricmc.api.ClientModInitializer;

/**
 * Client-side initialization for DisableVillagerTrade.
 * Currently no client-specific features, but kept for future expansion.
 */
public class DisableVillagerTradeFabricClient implements ClientModInitializer {
    
    @Override
    public void onInitializeClient() {
        // No client-specific initialization needed currently
        // This could be used for client-side config screen in the future
    }
}

